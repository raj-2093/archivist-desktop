import knex from "knex";
import { Database } from "sqlite3";

/**
 *
 * @param {Database} sqlConnIns
 * @param {knex.Knex} knexInstance
 */
export const handleProcesses = (sqlConnIns, knexInstance) => {};
